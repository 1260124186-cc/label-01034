# 自动化测试指南

## 测试覆盖范围

本自动化测试脚本覆盖了以下功能模块：

1. **用户管理模块**
   - 管理员登录
   - 教师注册与登录
   - 学生注册与登录
   - 管理员用户管理（修改用户信息）

2. **学院与专业管理模块**
   - 管理员创建学院
   - 管理员创建专业（关联学院）

3. **课程管理模块**
   - 教师创建课程
   - 课程信息设置（学分、学期、容量、时间）

4. **选课管理模块**
   - 学生浏览课程
   - 学生选课功能
   - 时间冲突检测
   - 选课容量限制

5. **报表导出模块**
   - 管理员导出课程报表

6. **权限控制模块**
   - 不同角色的菜单权限

## 使用方法

### 前提条件
- 已安装 Python 3.x
- 项目已正确配置（backend 目录存在）
- 已安装 SQLite3 和 g++（C++17 支持）

### 运行测试
```bash
# 在项目根目录运行
python3 automated_test.py
```

### 测试过程
1. 脚本会自动清理数据库（`make clean`）
2. 重新编译项目（`make`）
3. 依次运行所有测试用例
4. 每个测试用例都会：
   - 启动程序
   - 发送预定义的输入序列
   - 检查预期输出是否存在
   - 输出测试结果

## 测试用例详解

### 1. 管理员登录
- **输入**：登录菜单 -> 管理员账号（admin/admin123）-> 退出
- **预期输出**：显示管理员菜单、退出登录成功

### 2. 创建学院和专业
- **输入**：管理员登录 -> 学院管理 -> 创建学院 -> 专业管理 -> 创建专业
- **预期输出**：学院添加成功、专业添加成功

### 3. 注册教师
- **输入**：注册 -> 教师信息（用户名、姓名、密码、工号、学院ID）-> 登录验证
- **预期输出**：注册成功、显示教师菜单

### 4. 注册学生
- **输入**：注册 -> 学生信息（用户名、姓名、密码、学号、专业ID）-> 登录验证
- **预期输出**：注册成功、显示学生菜单

### 5. 教师创建课程
- **输入**：教师登录 -> 创建课程 -> 课程信息 -> 退出
- **预期输出**：课程创建成功

### 6. 学生选课
- **输入**：学生登录 -> 浏览课程 -> 选择课程
- **预期输出**：选课成功

### 7. 时间冲突检测
- **输入**：教师创建冲突课程 -> 学生尝试选课
- **预期输出**：时间冲突提示、选课失败

### 8. 管理员用户管理
- **输入**：管理员登录 -> 用户管理 -> 修改用户姓名
- **预期输出**：修改成功

### 9. 报表导出
- **输入**：管理员登录 -> 导出报表 -> 选择格式
- **预期输出**：导出成功提示

## 注意事项

1. **测试数据**：每个测试用例运行前都会清理数据库，确保测试环境干净
2. **超时处理**：每个测试用例有 15 秒超时限制，防止程序卡住
3. **输入验证**：测试中包含对输入格式的验证
4. **日志文件**：测试过程中会生成 `data/system.log` 日志文件

## 故障排除

### 编译失败
- 确保已安装 g++ 和 SQLite3
- 检查 Makefile 配置
- 运行 `make clean` 后重新编译

### 测试超时
- 检查程序是否有死循环
- 查看日志文件获取详细信息
- 手动运行程序检查是否正常

### 预期输出不匹配
- 检查程序输出格式是否有变化
- 查看实际输出内容
- 调整测试用例的预期输出

## 扩展测试

可以通过添加新的测试用例来扩展测试覆盖范围：

```python
{
    "name": "新测试名称",
    "input_sequence": ["输入1", "输入2", ...],
    "expected_outputs": ["预期输出1", "预期输出2", ...]
}
```

将新的测试用例添加到 `test_cases` 数组中即可。